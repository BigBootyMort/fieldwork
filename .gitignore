# ── Secrets / environment ─────────────────────────────────────────
.env
*.env
!.env.example

# Runtime secrets + state (never commit real API keys or per-run state)
backend/app/runtime_api_keys.json
backend/app/key_verifications.json
backend/app/inv_monitors.json
shell/backend/app/modules/agent/agent_config.json
shell/backend/app/modules/news/watchlist.json
shell/backend/app/modules/news/bias_ai_cache.json

# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
*.so
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# ── Docker / data volumes ─────────────────────────────────────────
*.log

# ── Runtime databases / vector stores (binary churn, not source) ──
ruvector.db
**/ruvector.db
*.db-wal
*.db-shm

# ── OS / editor ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.idea/
.vscode/
*.sublime-*

# ── Node (if ever added) ──────────────────────────────────────────
node_modules/

# ── Machine-local MCP / Claude Code settings ─────────────────────
# .mcp.json holds absolute host paths (leaks the OS username); keep it local.
.mcp.json
.claude/settings.local.json
# Local Claude Code state (worktrees, session/memory data, snapshots)
.claude/worktrees/
.claude/projects/
.claude/shell-snapshots/
.claude/todos/

# ── Scratch / legacy ─────────────────────────────────────────────
NEW/

# local avatar-render scratch (headless-chromium PNGs)
.render/

# ── Eval harness generated reports ───────────────────────────────
eval/report.md
eval/report.json
